Assistant must scrape all relevant information from the html document provided by the user.
Provide a JSON object listing the tools the assistant is going to use.
If there is not information, the assistant must place "null" in the correspondant propiety on the JSON response.

JSON DOCUMENT FORMAT:
```json
{
  model: string; // Without special characters
  description: Record<"en" | "es", string>; // Min 100 words max 200. Must have one per option in Record key, translate it in all the provided languages
  mainImgHref: string;  // Imagen principal del Mini Pc
  portsImgHref: string;  // Imagen de los puertos del Mini PC
  cpu: {
    brand: string; // Without special characters
    model: string; // Without special characters
    cores: number;
    threads: number;
    baseClockGHz?: number;
    boostClockGHz?: number;
    cache?: {
      type: string;
      capacityMB?: number;
    };
  };
  variants: {
    ram: {
      capacityGB: number;
      type: string;
    };
    storage: {
      type: string;
      capacityGB: number;
    };
    oferts: {
      provider: string; // Could be: BeeLink, AMD, ASUS, Geekom, MinisForum, Gigabyte, MundoNuc, Intel, Lenovo and HP
      url: string;
      priceUsd?: number;
      warrantyYears?: number;
    }[];
  }[];
  maxRAMCapacityGB?: number;
  maxStorageCapacityGB?: number;
  graphics: {
    integrated: boolean;
    brand: string; // Without special characters
    model: string; // Without special characters
    frequencyMHz?: number;
    graphicCoresCU?: number;
    maxTOPS?: number;
    displayPorts: {   
      thunderbolt?: {
        amount: number;
        type: string;    // Describe the type of the port and capabilities
      };
      dp?: {
        amount: number;
        type: string;   // Describe the type of the port and capabilities
      };
      hdmi?: {
        amount: number;
        type: string;   // Describe the type of the port and capabilities
      };
      usb4?: {
        amount: number;
        type: string;  // Describe the type of the port and capabilities
      };
    };
  };
  builtinMicrophone?: boolean;
  builtinSpeakers?: boolean;
  supportExternalDiscreteGraphicsCard?: boolean;
  connectivity: {
    wifi: string; // Describe the type and capabilities
    bluetooth: string; // Describe the type and capabilities
  };
  dimensions: {
    widthMm?: number;
    heightMm?: number;
    depthMm?: number;
    volumeL?: number;
  };
  weightKg: number;
  powerConsumptionW?: number;
  releaseYear?: number;
}
```